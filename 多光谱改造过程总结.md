# SegFormer 多光谱改造过程总结

本文档记录当前仓库从原始 RGB SegFormer 语义分割版本，改造成 Sentinel-2 多光谱光伏板语义分割版本的主要步骤和改动内容。

目标任务：

```text
输入：Sentinel-2 多光谱 tif 影像
输出：光伏板语义分割 mask
类别：background / photovoltaic
```

当前默认实验配置：

```text
band_mode = 6band
selected_bands = [1, 2, 3, 4, 5, 6]
in_channels = 6
num_classes = 2
input_shape = [256, 256]
model = SegFormer-B0
```

## 1. 新增统一多光谱配置

新增文件：

```text
multispectral_config.py
```

作用：

```text
统一管理 RGB / 4band / 6band 实验配置
统一管理输入影像后缀
统一管理输入通道数
统一管理 Sentinel-2 标准化参数
统一管理推理权重路径
```

主要配置：

```python
image_ext = ".tif"
band_mode = "6band"
selected_bands = [1, 2, 3, 4, 5, 6]
in_channels = 6
vis_bands = [3, 2, 1]
```

设计目的：

```text
后续切换 RGB / 4band / 6band 时，只需要改一个配置文件，避免 train.py、predict.py、mIoU 脚本之间参数不一致。
```

## 2. 修改 SegFormer 支持动态输入通道

修改文件：

```text
nets/backbone.py
nets/segformer.py
```

原始 SegFormer 默认输入是 RGB：

```text
in_channels = 3
```

改造后：

```python
SegFormer(..., in_channels=in_channels)
```

并将 `in_channels` 一路传入 MiT 主干的第一层：

```python
OverlapPatchEmbed(in_chans=in_channels)
```

这样模型可以支持：

```text
RGB    -> 3 通道
4band  -> 4 通道
6band  -> 6 通道
```

注意：

```text
这个改动只改变第一层 patch embedding 的输入通道数。
SegFormer 的 MiT encoder、多尺度特征输出、MLP decoder 仍保持原来的 SegFormer 结构。
```

## 3. 修改预训练权重加载逻辑

修改文件：

```text
nets/backbone.py
```

新增逻辑：

```python
load_backbone_weights(...)
```

原因：

```text
RGB 预训练权重的第一层输入通道是 3。
多光谱模型第一层输入通道可能是 4 或 6。
这会导致第一层 patch embedding 权重形状不匹配。
```

处理策略：

```text
形状匹配的权重正常加载
形状不匹配的权重跳过
多光谱第一层保持随机初始化，后续训练中学习
Transformer 主体其余权重尽量复用
```

这种策略适合论文中描述为：

```text
将 SegFormer 第一层输入通道扩展为 6，以适配多光谱影像；对于 RGB 预训练权重中形状不匹配的输入层权重不予加载，其余可匹配权重正常初始化。
```

## 4. 修改数据读取器支持 tif 多波段

修改文件：

```text
utils/dataloader.py
```

新增能力：

```text
读取 .jpg / .png 普通 RGB 图像
读取 .tif / .tiff 多光谱影像
按 selected_bands 选择指定波段
输出 HWC -> CHW 的训练张量
```

多光谱读取使用：

```python
rasterio
```

输入目录仍保持 VOC 风格：

```text
VOCdevkit/VOC2007/JPEGImages/xxx.tif
VOCdevkit/VOC2007/SegmentationClass/xxx.png
VOCdevkit/VOC2007/ImageSets/Segmentation/train.txt
VOCdevkit/VOC2007/ImageSets/Segmentation/val.txt
```

同时保留了 RGB 训练流程，便于做 RGB baseline。

## 5. 修改数据增强逻辑

修改文件：

```text
utils/dataloader.py
```

保留的增强：

```text
letterbox resize
随机缩放
随机长宽扰动
随机水平翻转
随机平移填充
高斯模糊
小角度旋转
```

针对多光谱禁用的增强：

```text
HSV 色彩扰动
```

原因：

```text
多光谱波段不是 RGB 颜色空间，B8/B11/B12 等波段没有 HSV 色彩含义。
如果强行做 HSV 增强，会破坏光谱信息。
```

另外，OpenCV 对 5 通道及以上图像不支持 `INTER_CUBIC` 的 `warpAffine`，因此多光谱旋转使用：

```python
cv2.INTER_LINEAR
```

RGB 仍保留原来的插值方式。

## 6. 修改输入标准化

修改文件：

```text
utils/utils.py
```

原始 RGB 标准化保持不变：

```python
(image - ImageNet mean) / ImageNet std
```

多光谱 tif 标准化新增：

```text
反射率缩放：image / 10000
按波段 clip
按波段 mean/std 标准化
```

入口函数变为：

```python
preprocess_input(image, is_multispectral=False)
```

为什么需要 `is_multispectral`：

```text
三波段 tif 和普通 RGB jpg 都可能是 3 通道。
仅靠通道数无法判断应该走 RGB 标准化还是 Sentinel-2 多光谱标准化。
```

因此 dataloader、推理和 mIoU 在读取 tif 时会显式传入：

```python
is_multispectral=True
```

## 7. 修改训练入口

修改文件：

```text
train.py
```

接入配置：

```python
from multispectral_config import (
    band_mode,
    image_ext,
    in_channels,
    normalization_config,
    selected_bands,
)
```

模型构建：

```python
SegFormer(..., in_channels=in_channels)
```

数据集构建：

```python
SegmentationDataset(
    ...,
    image_ext=image_ext,
    selected_bands=selected_bands,
)
```

训练日志中会打印：

```text
band_mode
image_ext
selected_bands
in_channels
normalization_config
```

这样每次训练都能确认当前实验到底是 RGB、4band 还是 6band。

## 8. 修改训练时验证回调

修改文件：

```text
utils/callbacks.py
```

主要改动：

```text
TensorBoard dummy input 使用真实 in_channels
EvalCallback 支持读取 tif
EvalCallback 支持 selected_bands
验证阶段使用和训练一致的多光谱预处理
```

原因：

```text
如果只改 train.py，不改 EvalCallback，训练到验证 mIoU 时仍会去读取 .jpg，导致多光谱训练中断。
```

## 9. 修改预测入口

修改文件：

```text
segformer.py
predict.py
```

`segformer.py` 新增多光谱推理能力：

```text
read_tif()
open_image()
resize_multiband_image()
multiband_to_rgb()
prepare_input()
predict_mask()
```

推理逻辑：

```text
PIL RGB 图像 -> 原 RGB 流程
tif 多光谱图像 -> rasterio 读取 selected_bands -> 多光谱预处理
```

可视化逻辑：

```text
模型使用全部 selected_bands 推理
显示结果时使用 vis_bands = [3, 2, 1] 生成真彩色底图
```

`predict.py` 中打开图像改为：

```python
segformer.open_image(path)
```

这样单张预测和文件夹预测都能自动处理 `.tif`。

## 10. 新增多光谱 mIoU 脚本

新增文件：

```text
多光谱get_miou.py
```

作用：

```text
读取 val.txt
读取 JPEGImages/xxx.tif
生成预测 mask
保存到 miou_out_multispectral/detection-results/
与 SegmentationClass/xxx.png 计算 mIoU
```

类别：

```python
name_classes = ["background", "photovoltaic"]
num_classes = 2
```

评估模式：

```python
miou_mode = 0  # 生成预测 + 计算 mIoU
miou_mode = 1  # 只生成预测
miou_mode = 2  # 只计算 mIoU
```

## 11. 修改模型结构统计

修改文件：

```text
summary.py
```

改造后会读取：

```python
band_mode
selected_bands
in_channels
```

并使用当前通道数统计模型：

```python
summary(model, (in_channels, H, W))
dummy_input = torch.randn(1, in_channels, H, W)
```

这样可以统计 3/4/6 通道版本的参数量和 FLOPs。

## 12. 补充依赖

修改文件：

```text
requirements.txt
```

新增：

```text
rasterio
thop
torchsummary
```

用途：

```text
rasterio      -> 读取多光谱 tif
thop          -> 统计 FLOPs
torchsummary  -> 打印模型结构
```

## 13. 新增使用说明

新增文件：

```text
README_多光谱使用说明.md
```

内容包括：

```text
配置文件说明
数据目录说明
训练命令
预测命令
mIoU 评估命令
summary 使用方式
推荐实验顺序
论文表述建议
```

## 14. 验证过程

改造过程中做过的关键验证：

```text
1. RGB 输入前向：
   [1, 3, 256, 256] -> [1, 2, 256, 256]

2. 6band 输入前向：
   [1, 6, 256, 256] -> [1, 2, 256, 256]

3. dataloader 合成 6 波段样本：
   image -> [6, 32, 32]
   label -> [32, 32]
   one-hot -> [32, 32, 3]

4. 多光谱验证回调：
   随机 6 波段输入 -> 单通道预测 mask

5. 多光谱推理：
   随机 6 波段输入 -> RGB 可视化结果

6. Python 语法检查：
   train.py
   segformer.py
   predict.py
   summary.py
   utils/dataloader.py
   utils/utils.py
   utils/callbacks.py
```

## 15. 当前使用方式概览

训练：

```bash
python train.py
```

预测：

```bash
python predict.py
```

多光谱 mIoU：

```bash
python 多光谱get_miou.py
```

模型结构统计：

```bash
python summary.py
```

## 16. 论文中建议表述

可以这样描述该对比模型：

```text
本文采用 SegFormer-B0 作为 Transformer-based 语义分割对比模型。为适配 Sentinel-2 多光谱影像，将 SegFormer 第一层 patch embedding 的输入通道数由 3 扩展为 6，对应 B2、B3、B4、B8、B11 和 B12 六个波段。除输入层通道数外，MiT 编码器和 MLP 解码器结构保持不变。对于 RGB 预训练权重中形状不匹配的输入层参数不进行加载，其余可匹配权重正常迁移。
```

英文表述可写为：

```text
SegFormer-B0 is adopted as a Transformer-based semantic segmentation baseline. To adapt it to Sentinel-2 multispectral imagery, the input channels of the first patch embedding layer are extended from 3 to 6, corresponding to B2, B3, B4, B8, B11, and B12. The remaining MiT encoder and MLP decoder are kept unchanged. For RGB pretrained weights, parameters with mismatched shapes in the input layer are skipped, while other compatible weights are loaded.
```
